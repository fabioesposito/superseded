# Reference Smolfile for running codex inside a smolvm microVM.
# See claude.smolfile for usage. Build first: docker/smolvm/build.sh codex

image = "superseded-smolvm-codex:latest"
net = true
workdir = "/workspace"

[secrets]
OPENAI_API_KEY = { from_env = "OPENAI_API_KEY" }
